<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Shout Out", root_path, class: 'navbar-brand' %>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% if logged_in? %>
          <li><a href='/boards'>My Boards</a></li>
          <li><a href='/boards/new'>New Board</a></li>
          <li><a href='/'>Post to Board</a></li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if logged_in? %>
          <li><a href='/logout'>Log Out</a></li>
        <% else %>
          <li><a href='/login'>Log In</a></li>
          <li><a href='/signup'>Sign Up</a></li>
        <% end %>
      </ul>
      <% if logged_in? %>
        <p class="navbar-text navbar-right">Hello <a href='#'><%= current_user.first_name %></a></p>
      <% end %>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
